<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.awsyogya">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".isi_rain"
            android:parentActivityName=".main"/>
        <activity android:name=".isi_wind"
            android:parentActivityName=".main"/>
        <activity android:name=".isi_air"
            android:parentActivityName=".main"/>
        <activity android:name=".isi_turf"
            android:parentActivityName=".main"/>
        <activity android:name=".isi_solar"
            android:parentActivityName=".main"/>
        <activity android:name=".isi_bare"
            android:parentActivityName=".main"/>
        <activity
            android:name=".main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_sejarah"
            android:parentActivityName=".main"/>
        <activity android:name=".activity_visimisi"
            android:parentActivityName=".main"/>
        <activity android:name=".activity_tugas"
            android:parentActivityName=".main"></activity>
    </application>

</manifest>